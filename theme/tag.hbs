{{!< default}}

<div class="gh-outer">
<main class="site-main gh-inner">
    {{#tag}}{{> "tag-filter" active_tag_slug=slug}}{{/tag}}

    <div class="post-feed gh-feed">
        {{#foreach posts}}
            {{> "loop"}}
        {{/foreach}}
    </div>

</main>
</div>
